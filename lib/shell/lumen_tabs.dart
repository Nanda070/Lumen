/// Shell tab indices — keep in sync with [AppShell] page order.
abstract final class LumenTabs {
  static const today = 0;
  static const calendar = 1;
  static const tasks = 2;
  static const finance = 3;
  static const more = 4;
}
